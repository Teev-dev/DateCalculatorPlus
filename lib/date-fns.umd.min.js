import { format, parseISO, addDays, startOfDay, getDay, isBefore, isEqual, addMonths } from 'date-fns';

const dateFns = {
    format,
    parseISO,
    addDays,
    startOfDay,
    getDay,
    isBefore,
    isEqual,
    addMonths
};

export default dateFns;
